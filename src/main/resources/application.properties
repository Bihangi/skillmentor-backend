# SkillMentor Backend Configuration

# Server
server.port=8080

# PostgreSQL Database
spring.datasource.url=${DATABASE_URL:jdbc:postgresql://localhost:5432/skillmentor}
spring.datasource.username=${DATABASE_USERNAME:postgres}
spring.datasource.password=${DATABASE_PASSWORD:248dodamwala#}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Clerk JWT Authentication
spring.security.oauth2.resourceserver.jwt.jwk-set-uri=${CLERK_JWKS_URL:https://viable-guppy-78.clerk.accounts.dev/.well-known/jwks.json}
spring.security.oauth2.resourceserver.jwt.issuer-uri=${CLERK_ISSUER_URL:https://viable-guppy-78.clerk.accounts.dev}

# CORS
app.cors.allowed-origins=${CORS_ORIGINS:http://localhost:5173,http://localhost:3000}

# Swagger
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui.html

# Seed data on first run
spring.sql.init.mode=always
spring.sql.init.continue-on-error=true

# SMTP config
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=bihangi.y@gmail.com
spring.mail.password=nkrv ydhz qtiu ncxh
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.properties.mail.smtp.connectiontimeout=5000
spring.mail.properties.mail.smtp.timeout=5000
spring.mail.properties.mail.smtp.writetimeout=5000